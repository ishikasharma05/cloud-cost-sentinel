"""
baseline.py  (Week 2, optional split-out from detector.py)

TODO: Compute rolling averages per service from stored history, e.g.:

def rolling_average(records: list[dict], service: str, window_days: int = 7) -> float:
    ...
"""
