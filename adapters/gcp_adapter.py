"""
gcp_adapter.py  (Week 5+, once AWS is solid)

TODO: Fetch GCP billing data (Cloud Billing Budgets API or BigQuery
billing export) and normalize it into the same format as aws_adapter.py:

    {"provider": "gcp", "service": ..., "date": ..., "cost": ...}
"""
